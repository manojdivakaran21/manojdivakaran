<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shivaprakashys.heavenly_kerala">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity"  />
        <activity android:name=".beaches"android:label="Beaches" />
        <activity android:name=".hillstation"android:label="Hill station" />
        <activity android:name=".adventure"android:label="Adventure" />
        <activity android:name=".religious"android:label="Religious">
        </activity>
    </application>

</manifest>